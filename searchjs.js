const languageSelect = document.getElementById('stream-language');
const searchButton = document.querySelector('.search2 button');

searchButton.addEventListener('click', () => {
  const selectedLanguage = languageSelect.value;
  // Hier kannst du weitere Aktionen ausführen, um die Suche nach Sprache zu verarbeiten
});
